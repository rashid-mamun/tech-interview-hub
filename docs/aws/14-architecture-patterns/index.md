---
sidebar_position: 1
title: Architecture Patterns
---

## 89. What is AWS App Mesh (service mesh), and what problem does a sidecar proxy solve?

### What cross-cutting concern (retries, mTLS, observability) does a sidecar handle so app code doesn't have to?

## 90. What is the circuit breaker pattern, and how would you implement it in an AWS microservices architecture to prevent cascading failures?

### What state transitions does a circuit breaker go through (closed → open → half-open)?

## 91. What is the difference between client-side and server-side service discovery, and how does AWS Cloud Map or ECS Service Discovery support it?

### What's the trade-off in client complexity between the two approaches?
